$(window).load(init);

function init() {
    switch (sv.pageID) {
        case "home":
            home.initialize();
            break;
    }
}

var home = {
    initialize: function () {
        // initialize things here
    },

    fetchItems: function () {
        var number_of_items = 8;

        $.ajax({
            type: "GET",
            async: true,
            url: "",
            success: function () {
            },
            error: function () {
            }
        });
    },

    addItem: function () {
        // add 4 items in one row, next four items in another row
    },

    makeItem: function () {
        var $item = utilities.createElement("<div/>", null, { 'class': "col-md-3 col-sm-4 col-xs-6" });
        var $el = utilities.createElement("<div/>", $item, { 'class': "item" });
        utilities.createElement("<div/>", $el, { 'class': "item-icon", 'html': "<span>HOT</span>" });
        utilities.createElement("<div/>", $el, { 'class': "item-image", 'html': '<a href="#"><img src="../images/items/2.png" alt="" class="img-responsive"/></a>' });

        $el = utilities.createElement("<div/>", $el, { 'class': "item-details" });
        utilities.createElement("<h5/>", $el, { 'html': '<a href="#">HTC One V</a><div class="clearfix"></div>' });
        utilities.createElement("<p/>", $el, { 'html': "Something about the product goes here. Not More than 2 lines.<hr />" });
        utilities.createElement("<div/>", $el, { 'class': "item-price pull-left", 'html': '₹360' });

        // add to cart button
        utilities.createElement("<div/>", $el, { 'class': "pull-right", 'html': '<a href="#" class="btn btn-danger btn-sm">Add to Cart</a>' });
        utilities.createElement("<div/>", $el, { 'class': "clearfix" });

        return $item;
    }
};